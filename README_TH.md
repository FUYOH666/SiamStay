# 🏡 SiamStay

[![Python 3.12](https://img.shields.io/badge/python-3.12-blue.svg)](https://www.python.org/downloads/)
[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](https://opensource.org/licenses/MIT)
[![FastAPI](https://img.shields.io/badge/FastAPI-0.104.1-green.svg)](https://fastapi.tiangolo.com/)
[![PostgreSQL](https://img.shields.io/badge/PostgreSQL-14+-blue.svg)](https://www.postgresql.org/)

<div align="center">

**🌍 Language:** [🇷🇺 Русский (Main)](README.md) | [🇺🇸 English](README_EN.md) | [🇹🇭 ไทย](README_TH.md)

</div>

---

**แพลตฟอร์มจัดการการเช่าวิลล่าในประเทศไทยพร้อมการลงทะเบียน TM30 อัตโนมัติ**

SiamStay - แพลตฟอร์มที่ครอบคลุมสำหรับการจัดการการเช่าวิลล่าในประเทศไทย (30+ วัน) พร้อมการลงทะเบียนแขกอัตโนมัติผ่าน TM30 API

---

## ฟีเจอร์หลัก

- ✅ **เน้นที่วิลล่า** - การจัดการทรัพย์สินที่เรียบง่ายสำหรับวิลล่าเท่านั้น
- ✅ **การบูรณาการ TM30** - การลงทะเบียนแขกอัตโนมัติกับกรมการตรวจคนเข้าเมืองไทย
- ✅ **ขั้นต่ำ 30+ วัน** - การปฏิบัติตามกฎหมายการเช่าของไทยอย่างสมบูรณ์
- ✅ **ส택ที่ทันสมัย** - Python 3.12, FastAPI, PostgreSQL, Redis
- ✅ **ข้อมูลประจำตัวที่เข้ารหัส** - การเก็บข้อมูลประจำตัวเจ้าของ TM30 อย่างปลอดภัย
- ✅ **การตรวจสอบสุขภาพ** - การตรวจสอบสุขภาพและเมตริก Prometheus
- ✅ **API-First** - RESTful API พร้อมเอกสาร OpenAPI

---

## ความต้องการ

- **Python 3.12** - เวอร์ชันเดียวที่รองรับ
- **PostgreSQL 14+** - ฐานข้อมูล
- **Redis 6+** - การแคชและคิว
- **uv** - ตัวจัดการแพ็คเกจ (ติดตั้งอัตโนมัติ)

---

## การติดตั้ง

### 1. โคลน Repository

```bash
git clone https://github.com/FUYOH666/SiamStay.git
cd SiamStay
```

### 2. ติดตั้ง Dependencies

```bash
# ติดตั้ง uv (หากยังไม่ได้ติดตั้ง)
curl -LsSf https://astral.sh/uv/install.sh | sh

# ติดตั้ง dependencies ของโปรเจ็กต์
uv sync
```

### 3. การกำหนดค่า

```bash
# คัดลอกไฟล์ .env ตัวอย่าง
cp .env.example .env

# แก้ไขไฟล์ .env และกรอกค่าที่จำเป็น
# ⭐️ สำคัญ: DATABASE_URL, REDIS_URL, TM30_ENCRYPTION_KEY (หากเปิดใช้งาน TM30)
```

### 4. การตั้งค่าฐานข้อมูล

```bash
# สร้างฐานข้อมูล PostgreSQL
createdb siamstay

# รัน migrations
uv run alembic upgrade head
```

---

## การกำหนดค่า

### การตั้งค่าหลัก (config.yaml)

การตั้งค่าหลักอยู่ใน `config.yaml` ความลับและการแทนที่ผ่านตัวแปรสภาพแวดล้อมใน `.env`

### ตัวแปรสภาพแวดล้อม (.env)

**จำเป็น:**
- `DATABASE_URL` - PostgreSQL connection string
- `REDIS_URL` - Redis connection string

**สำหรับการบูรณาการ TM30:**
- `TM30_ENABLED=true` - เปิดใช้งานการบูรณาการ TM30
- `TM30_ENCRYPTION_KEY` - คีย์การเข้ารหัสสำหรับข้อมูลประจำตัว TM30 (32-byte base64)

**สำหรับการชำระเงิน:**
- `STRIPE_SECRET_KEY` - Stripe secret key
- `STRIPE_PUBLIC_KEY` - Stripe public key

**สำหรับการแจ้งเตือน:**
- `SMTP_HOST`, `SMTP_PORT`, `SMTP_USER`, `SMTP_PASSWORD` - การตั้งค่า SMTP

### สร้าง TM30 Encryption Key

```bash
python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
```

เพิ่มคีย์ที่สร้างไปยัง `.env` เป็น `TM30_ENCRYPTION_KEY`

---

## การรัน

### Development

```bash
# รัน FastAPI server
uv run uvicorn app.api.main:app --reload --host 0.0.0.0 --port 8000
```

### Production

```bash
# ใช้ gunicorn
uv run gunicorn app.api.main:app -w 4 -k uvicorn.workers.UvicornWorker --bind 0.0.0.0:8000
```

### Docker

```bash
# สร้าง image
docker build -t siamstay .

# รัน container
docker run -p 8000:8000 --env-file .env siamstay
```

---

## API Documentation

หลังจากเริ่มแอปพลิเคชัน:

- **Swagger UI**: http://localhost:8000/docs
- **ReDoc**: http://localhost:8000/redoc
- **Health Check**: http://localhost:8000/healthz
- **Metrics**: http://localhost:8000/metrics

---

## Endpoints หลัก

### Properties (วิลล่า)

- `POST /properties` - สร้างวิลล่า
- `GET /properties/{id}` - รับข้อมูลวิลล่า
- `GET /properties/{id}/availability` - ตรวจสอบความพร้อมใช้งาน
- `GET /properties/{id}/price` - คำนวณราคา
- `POST /properties/{id}/validate-compliance` - ตรวจสอบความสอดคล้อง

### Bookings

- `POST /bookings` - สร้างการจอง
- `GET /bookings/{id}` - รับข้อมูลการจอง
- `POST /bookings/{id}/confirm` - ยืนยันการจอง
- `POST /bookings/{id}/check-in` - เช็คอินแขก

### TM30

- `POST /tm30/bookings/{id}/register` - ลงทะเบียนแขกผ่าน TM30
- `GET /tm30/bookings/{id}/status` - รับสถานะการลงทะเบียน TM30

---

## การบูรณาการ TM30

### ตั้งค่าข้อมูลประจำตัว TM30 สำหรับเจ้าของ

1. เจ้าของให้ข้อมูลล็อกอินและรหัสผ่านสำหรับระบบ TM30
2. ข้อมูลประจำตัวถูกเข้ารหัสและเก็บไว้ในฐานข้อมูล
3. การลงทะเบียนอัตโนมัติเกิดขึ้นผ่าน TM30 API เมื่อแขกเช็คอิน

### ปิดการใช้งานการบูรณาการ TM30

หากไม่ต้องการการบูรณาการ TM30 ตั้งค่าใน `config.yaml`:

```yaml
tm30:
  enabled: false
```

---

## การทดสอบ

```bash
# รันการทดสอบทั้งหมด
uv run pytest

# รันพร้อม coverage
uv run pytest --cov=app --cov-report=html

# รันการทดสอบเฉพาะ
uv run pytest tests/test_properties.py
```

---

## โครงสร้างโปรเจ็กต์

```
Siamstay/
├── app/
│   ├── api/              # FastAPI endpoints
│   ├── core/             # Business logic
│   ├── integrations/      # External integrations (TM30, payments)
│   ├── models/           # SQLAlchemy models
│   ├── schemas/          # Pydantic schemas
│   ├── services/         # Services (notifications, encryption)
│   ├── db/               # Database
│   └── utils/            # Utilities
├── tests/                # Tests
├── alembic/              # Database migrations
├── config.yaml           # Configuration
├── pyproject.toml        # Dependencies
└── README.md            # Documentation
```

---

## ปัญหาที่พบบ่อย

### ข้อผิดพลาด: "TM30_ENCRYPTION_KEY must be set"

**วิธีแก้:** สร้างคีย์และเพิ่มไปยัง `.env`:
```bash
python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
```

### ข้อผิดพลาด: "Database connection failed"

**วิธีแก้:** ตรวจสอบ `DATABASE_URL` ใน `.env` และตรวจสอบว่า PostgreSQL กำลังทำงาน

### ข้อผิดพลาด: "Minimum stay is 30 days"

**วิธีแก้:** นี่คือข้อกำหนดทางกฎหมายของไทย การจองต้องอยู่ที่อย่างน้อย 30 วัน

---

## License

MIT License

---

## ติดต่อ

**ผู้เขียน:** Aleksandr Mordvinov

- **Email**: @scanovich_ai
- **GitHub**: [FUYOH666](https://github.com/FUYOH666)

---

**💡 SiamStay - การทำงานอัตโนมัติของการเช่าวิลล่าในประเทศไทยพร้อมการลงทะเบียน TM30 อัตโนมัติ**
